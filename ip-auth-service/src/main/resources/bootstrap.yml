spring:
  application:
    name: ip-auth-service

  cloud:
    nacos:
      discovery:
        server-addr: localhost:20000
      config:
        server-addr: localhost:20000

  config:
    import:
      - optional:nacos:ip-auth-service.yaml
